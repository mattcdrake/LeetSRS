# Separate GitHub authentication from Gist setup and sync

Issue: [#295](https://github.com/mattcdrake/LeetSRS/issues/295) · Parent: [#298](https://github.com/mattcdrake/LeetSRS/issues/298)

Step 1 of 4: change ownership while preserving behavior. Sync execution cleanup, the new setup UI, sync/backup separation, and OAuth belong to later work.

## Tasks

- [x] Add `services/github-auth.ts` with ordinary functions for credential access, removal, PAT validation, credential readiness, and authenticated-client acquisition using `infrastructure/github/client.ts`. Keep credential persistence behind this boundary using the existing `githubPat` metadata key. Support the supplied PAT used by validation adapters as well as stored credentials, without persisting validation inputs or adding authentication requests to Gist operations. Add focused auth tests preserving blank-input handling, untrimmed tokens, and existing validation results/errors.
- [ ] Extract destination configuration, client-consuming Gist validation, and Gist creation into `services/gist-setup.ts`; have ongoing sync in `services/github-sync.ts` acquire its client through auth and read destination configuration through setup. Migrate `entrypoints/background/message-handlers.ts` and other internal callers to the owning auth/setup/sync modules; remove superseded exports and forwarding adapters from `github-sync.ts`, including the `validatePat` re-export. Preserve the existing `GistSyncConfig`/RPC inputs and results used by the popup, with each handler wired to the owning services. Preserve PAT → Gist ID → enabled write order, partial-update defaults, validation with the supplied PAT, and creation's client → export → localization → request → destination/status persistence order. Keep the setup module independent of the sync service to avoid a cycle.
- [ ] Route alarm readiness in `entrypoints/background/index.ts` through auth readiness and setup configuration, preserving the enabled/credential/Gist-ID gate, startup wait, shared write queue, interval, and badge fallback. Add background regression coverage for ready, disabled, missing-credential, and missing-destination cases; keep the readiness check free of network requests.
- [ ] Route credential preservation/restoration in `services/import-export.ts` through auth, retaining reset's credential-removal position and import's read → reset → truthy-credential restore sequence. Retain and adapt `services/__tests__/import-export.test.ts` and `services/__tests__/import-export-characterization.test.ts` to verify missing/empty PAT behavior, ignored imported credentials, partial failures, exact persistence order, and unchanged exported JSON.
- [ ] Adapt `services/__tests__/github-sync.test.ts` and add focused setup and message-handler coverage for the extracted boundaries. Migrate test imports and mocks to the owning modules; verify existing RPC behavior without retaining the old service exports. Preserve existing checks for private Gist creation, exact payloads, push/pull/no-change decisions, missing configuration, error/status behavior, concurrency, timestamps, and partial failures. Verify setup can use a supplied client without credential access and each Gist operation retains one client across requests; reuse `test/utils/deferred.ts` where needed.
- [ ] Manually exercise the existing popup in a test profile: validate invalid and valid PATs, connect an existing Gist, create a private Gist, reopen settings, enable sync, and use Sync Now. Check existing messages and persisted fields, successful push/pull/no-change results, automatic sync on the one-minute alarm, and skipped automatic sync when disabled or incompletely configured. Export/import test data and verify the PAT survives import but is absent from the payload; reset and verify credentials/configuration are cleared.

## Acceptance criteria

- Authentication owns credential access, PAT validation, and client acquisition; Gist setup owns destination configuration and consumes an authenticated client, as does sync. Internal callers import from the owning modules; `github-sync.ts` has no auth/setup re-exports or forwarding compatibility adapters. Infrastructure remains independent of services, and service dependencies remain acyclic.
- Existing PAT connections, popup interactions, RPC inputs/results, errors, sync policy, queue behavior, and persistence order remain compatible, including partial failures and untrimmed validation inputs.
- Storage keys, schema, backup and Gist payloads remain unchanged. Import preserves the existing truthy PAT, standalone reset removes credentials, and exports omit credentials.
- Relevant auth, setup, sync, background, and import/export regression tests and `npm run check` pass; the manual scenarios above produce the expected observations. Run tests and `npm run check` with the repository-required sandbox escalation.
